<%= render 'errors' %>
<div class="container">
  <div class="card">
    <div class="card-header">
      <h1>Edit <%= @listing.name  %></h1>
    </div>

    <div class="card-body">
      <%= render 'form' %>
    </div>
    <div class="card-footer">
      <%= link_to 'back', listing_path(@listing) %>
    </div>
  </div>
</div>